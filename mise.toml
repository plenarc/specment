[tools]
node = "lts"
pnpm = "latest"

[settings]
# 自動アップデートを有効化
auto_install = true

[tasks.pnpm-update]
description = "Update pnpm to latest version"
run = "mise install pnpm@latest"

[hooks]
# プロジェクト開始時に最新版に更新
enter = "mise install pnpm@latest"
